---
phase: 02-ics-feeds-and-calendar-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sync/IcsSyncAdapter.ts
  - src/sync/SyncManager.ts
  - tests/sync/IcsSyncAdapter.test.ts
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - SYNC-03
  - EVNT-03
  - EVNT-04

must_haves:
  truths:
    - "ICS feed URL can be fetched and parsed into CalendarEvent[] objects"
    - "Recurring events (RRULE) are expanded into individual instances within a given date range"
    - "All event times are converted to local timezone regardless of source VTIMEZONE"
    - "Malformed ICS feeds produce meaningful error messages, not crashes"
    - "SyncManager delegates ICS sources to IcsSyncAdapter and stores results in EventStore"
  artifacts:
    - path: "src/sync/IcsSyncAdapter.ts"
      provides: "ICS fetch, parse, RRULE expansion, timezone conversion"
      exports: ["IcsSyncAdapter"]
    - path: "tests/sync/IcsSyncAdapter.test.ts"
      provides: "Unit tests for ICS parsing and RRULE expansion"
  key_links:
    - from: "src/sync/IcsSyncAdapter.ts"
      to: "ical.js"
      via: "ICAL.parse, ICAL.Component, ICAL.Event, ICAL.Time"
      pattern: "ICAL\\.parse|ICAL\\.Component|ICAL\\.Event"
    - from: "src/sync/SyncManager.ts"
      to: "src/sync/IcsSyncAdapter.ts"
      via: "SyncManager calls IcsSyncAdapter.sync() for ICS sources"
      pattern: "IcsSyncAdapter"
    - from: "src/sync/SyncManager.ts"
      to: "src/store/EventStore.ts"
      via: "SyncManager calls eventStore.replaceEvents() with parsed events"
      pattern: "replaceEvents"
---

<objective>
Create the ICS sync adapter that fetches ICS feeds, parses them with ical.js, expands recurring events (RRULE) on-demand within a date range, converts all times to local timezone, and wire it into SyncManager so ICS sources actually sync.

Purpose: This is the data pipeline foundation -- without real ICS parsing, no events can appear in any calendar view.
Output: Working IcsSyncAdapter class, updated SyncManager that delegates to it, unit tests.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-ics-feeds-and-calendar-views/02-CONTEXT.md
@.planning/phases/02-ics-feeds-and-calendar-views/02-RESEARCH.md

<interfaces>
<!-- Key types the executor needs from the existing codebase -->

From src/models/types.ts:
```typescript
export interface CalendarEvent {
  id: string;                    // Format: `${sourceId}::${eventUid}`
  sourceId: string;
  title: string;
  start: string;                 // ISO 8601 datetime
  end: string;                   // ISO 8601 datetime
  allDay: boolean;
  location?: string;
  description?: string;
  recurrenceId?: string;
}

export interface CalendarSource {
  id: string;
  name: string;
  type: 'google' | 'caldav' | 'ics';
  color: string;
  enabled: boolean;
  ics?: { feedUrl: string; };
}

export type SyncState =
  | { status: 'idle'; lastSyncTime: number | null }
  | { status: 'syncing'; startedAt: number }
  | { status: 'error'; message: string; lastSyncTime: number | null };
```

From src/store/EventStore.ts:
```typescript
export class EventStore {
  replaceEvents(sourceId: string, newEvents: CalendarEvent[]): void;
  getEventsForDate(dateStr: string): CalendarEvent[];
}
```

From src/sync/SyncManager.ts:
```typescript
export class SyncManager {
  constructor(onStateChange: (state: SyncState) => void);
  async syncAll(sources: CalendarSource[]): Promise<void>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install ical.js and create IcsSyncAdapter with tests</name>
  <files>package.json, src/sync/IcsSyncAdapter.ts, tests/sync/IcsSyncAdapter.test.ts</files>
  <read_first>
    - src/models/types.ts (CalendarEvent interface, CalendarSource interface)
    - src/sync/SyncManager.ts (current sync stub to understand integration point)
    - tests/sync/SyncManager.test.ts (test patterns used in this project)
    - package.json (current dependencies)
  </read_first>
  <behavior>
    - Test 1: parseIcsText() with a simple single-event ICS string returns CalendarEvent[] with correct id (`${sourceId}::${uid}`), title, start (ISO 8601 local), end (ISO 8601 local), allDay=false, location, description
    - Test 2: parseIcsText() with an all-day VEVENT (DTSTART;VALUE=DATE:20260401) returns allDay=true and start/end as date-only ISO strings
    - Test 3: parseIcsText() with RRULE (FREQ=WEEKLY;COUNT=4) and rangeStart/rangeEnd covering 2 occurrences returns exactly 2 CalendarEvent instances with unique recurrenceId values
    - Test 4: parseIcsText() with VTIMEZONE (America/New_York) converts times to local timezone via ICAL.Time.toJSDate()
    - Test 5: parseIcsText() with invalid/empty ICS text throws Error with message containing "解析失败"
    - Test 6: Events outside the specified date range are excluded from results
  </behavior>
  <action>
    1. Run `npm install ical.js` to add the library. Also run `npm install --save-dev @types/ical.js` if types package exists (check npm registry; if not, create a minimal ambient declaration in `src/types/ical.js.d.ts`).

    2. Create `src/sync/IcsSyncAdapter.ts` with:
       - `export class IcsSyncAdapter`
       - Method `parseIcsText(icsText: string, sourceId: string, rangeStart: Date, rangeEnd: Date): CalendarEvent[]` (synchronous, pure parsing logic, no network)
       - Method `async sync(source: CalendarSource, rangeStart: Date, rangeEnd: Date): Promise<CalendarEvent[]>` (fetches via `requestUrl({ url: source.ics!.feedUrl })` then delegates to parseIcsText)
       - Private helper `toCalendarEvent(icalEvent: ICAL.Event, sourceId: string, occurrence?: ICAL.Time): CalendarEvent` that maps:
         - `id`: `${sourceId}::${icalEvent.uid}` (append `::${occurrence.toICALString()}` if recurring instance)
         - `title`: `icalEvent.summary`
         - `start`: occurrence ? `occurrence.toJSDate().toISOString()` : `icalEvent.startDate.toJSDate().toISOString()` (per D-10, ical.js toJSDate converts to local)
         - `end`: compute from occurrence + duration, or `icalEvent.endDate.toJSDate().toISOString()`
         - `allDay`: `icalEvent.startDate.isDate` (boolean)
         - `location`: `icalEvent.location || undefined`
         - `description`: `icalEvent.description || undefined`
         - `recurrenceId`: occurrence ? `occurrence.toICALString()` : undefined
       - For RRULE expansion (per D-09): use `icalEvent.iterator(ICAL.Time.fromJSDate(rangeStart))`, iterate until `next.compare(ICAL.Time.fromJSDate(rangeEnd)) > 0`
       - Error handling: wrap ICAL.parse in try/catch, throw `new Error('日历数据解析失败: ' + source.ics?.feedUrl + '. 该链接可能不是有效的ICS订阅源.')`
       - For network errors in sync(): catch and throw `new Error('无法连接到日历源: ' + source.ics?.feedUrl + '. 请检查网络连接和URL是否正确.')`

    3. Create `tests/sync/IcsSyncAdapter.test.ts` using vitest. Test parseIcsText() directly (no network mocking needed). Construct ICS text strings inline for test fixtures:
       ```
       const SIMPLE_ICS = `BEGIN:VCALENDAR\nBEGIN:VEVENT\nUID:test-1\nDTSTART:20260401T100000Z\nDTEND:20260401T113000Z\nSUMMARY:Team standup\nLOCATION:Room 3A\nDESCRIPTION:Daily sync\nEND:VEVENT\nEND:VCALENDAR`;
       ```
  </action>
  <verify>
    <automated>cd C:/OneDrive/src/UniCalendar && npx vitest run tests/sync/IcsSyncAdapter.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "ical.js" in dependencies
    - src/sync/IcsSyncAdapter.ts contains `export class IcsSyncAdapter`
    - src/sync/IcsSyncAdapter.ts contains `parseIcsText(icsText: string, sourceId: string, rangeStart: Date, rangeEnd: Date): CalendarEvent[]`
    - src/sync/IcsSyncAdapter.ts contains `async sync(source: CalendarSource, rangeStart: Date, rangeEnd: Date): Promise<CalendarEvent[]>`
    - src/sync/IcsSyncAdapter.ts contains `ICAL.parse`
    - src/sync/IcsSyncAdapter.ts contains `event.iterator`
    - src/sync/IcsSyncAdapter.ts contains `toJSDate`
    - tests/sync/IcsSyncAdapter.test.ts contains at least 5 `it(` or `test(` calls
    - `npx vitest run tests/sync/IcsSyncAdapter.test.ts` exits 0
  </acceptance_criteria>
  <done>IcsSyncAdapter parses ICS text into CalendarEvent[], expands RRULE within date ranges, converts timezones, handles errors with Chinese messages. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire IcsSyncAdapter into SyncManager with EventStore integration</name>
  <files>src/sync/SyncManager.ts, src/main.ts</files>
  <read_first>
    - src/sync/SyncManager.ts (current stub implementation)
    - src/sync/IcsSyncAdapter.ts (just created in Task 1)
    - src/store/EventStore.ts (replaceEvents method)
    - src/main.ts (triggerSync and onSyncStateChange flow)
    - src/models/types.ts (CalendarSource type check)
  </read_first>
  <action>
    1. Update `src/sync/SyncManager.ts`:
       - Add `import { IcsSyncAdapter } from './IcsSyncAdapter';`
       - Add `import { EventStore } from '../store/EventStore';`
       - Change constructor to accept `eventStore: EventStore` as second parameter: `constructor(onStateChange: (state: SyncState) => void, eventStore: EventStore)`
       - Store as `private eventStore: EventStore`
       - Create `private icsAdapter = new IcsSyncAdapter()`
       - In `syncAll()`, replace the stub `Promise.all(sources.map(() => Promise.resolve()))` with real logic:
         - Compute date range: 3 months before and after now (`new Date(); start.setMonth(start.getMonth() - 3); end.setMonth(end.getMonth() + 3)`)
         - Filter to enabled sources only: `sources.filter(s => s.enabled)`
         - For each source, if `source.type === 'ics'`: call `this.icsAdapter.sync(source, rangeStart, rangeEnd)`
         - For other types: skip (Phase 3/4 will add adapters)
         - After each source sync: call `this.eventStore.replaceEvents(source.id, events)`
         - Use `Promise.allSettled()` to allow per-source errors without failing the entire sync
         - If any source fails: collect error messages, set state to error with combined message, but still complete successful sources
         - If all succeed: set state to idle with lastSyncTime

    2. Update `src/main.ts`:
       - Change SyncManager construction from `new SyncManager((state) => this.onSyncStateChange(state))` to `new SyncManager((state) => this.onSyncStateChange(state), this.eventStore)`
       - After `triggerSync()` completes, call `this.refreshCalendarViews()` (new private method)
       - Add `private refreshCalendarViews(): void` that iterates open CalendarView leaves and calls a new `rerender()` method on them (this method will be added in Plan 02-03/04, for now just call `updateSyncStatus` which already exists)

    3. Update existing SyncManager tests if they break due to new constructor signature: add `new EventStore()` as second arg.
  </action>
  <verify>
    <automated>cd C:/OneDrive/src/UniCalendar && npx vitest run --reporter=verbose && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/sync/SyncManager.ts contains `import { IcsSyncAdapter }`
    - src/sync/SyncManager.ts contains `import { EventStore }`
    - src/sync/SyncManager.ts constructor has `eventStore: EventStore` parameter
    - src/sync/SyncManager.ts contains `source.type === 'ics'`
    - src/sync/SyncManager.ts contains `this.icsAdapter.sync(`
    - src/sync/SyncManager.ts contains `this.eventStore.replaceEvents(`
    - src/sync/SyncManager.ts contains `Promise.allSettled`
    - src/main.ts contains `new SyncManager((state) => this.onSyncStateChange(state), this.eventStore)`
    - `npx vitest run` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>SyncManager detects ICS sources, delegates to IcsSyncAdapter, stores results in EventStore, handles per-source errors gracefully. Build and all tests pass.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds — TypeScript compiles, esbuild bundles
2. `npx vitest run` — all tests pass including new IcsSyncAdapter tests
3. IcsSyncAdapter.parseIcsText returns correct CalendarEvent[] for single events, recurring events, and all-day events
4. SyncManager.syncAll with an ICS source calls IcsSyncAdapter and stores events in EventStore
</verification>

<success_criteria>
- ICS feeds can be parsed into CalendarEvent objects with correct fields
- RRULE expansion produces correct number of instances within date range
- Timezone conversion works (toJSDate produces local time)
- SyncManager integrates IcsSyncAdapter for real ICS syncing
- Error messages are Chinese-language per UI-SPEC copywriting contract
- All existing + new tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-ics-feeds-and-calendar-views/02-01-SUMMARY.md`
</output>
