---
phase: 09-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/CalendarView.ts
  - src/lunar/LunarService.ts
  - tests/sync/IcsSyncAdapter.test.ts
  - tests/sync/GoogleSyncAdapter.test.ts
autonomous: true
requirements:
  - VIEW-02
  - VIEW-03
  - VIEW-04
  - VIEW-05
  - VIEW-06
  - SYNC-03
  - EVNT-01
  - EVNT-02
  - EVNT-03
  - EVNT-04

must_haves:
  truths:
    - "CalendarView reads settings.defaultView on open instead of hardcoding 'month'"
    - "getLunarMonthForTitle() prepends '闰' for leap months"
    - "All-day event end date tests pass (inclusive end date convention)"
    - "All 97 tests pass with 0 failures"
  artifacts:
    - path: "src/views/CalendarView.ts"
      provides: "defaultView setting wired to currentViewMode"
      contains: "this.plugin.settings.defaultView"
    - path: "src/lunar/LunarService.ts"
      provides: "Leap month prefix in toolbar title"
      contains: "lunar.isLeap"
    - path: "tests/sync/IcsSyncAdapter.test.ts"
      provides: "Fixed all-day event end date test"
      contains: "2026-04-01"
    - path: "tests/sync/GoogleSyncAdapter.test.ts"
      provides: "Fixed all-day event end date test"
      contains: "2026-04-05"
  key_links:
    - from: "src/settings/SettingsTab.ts"
      to: "src/views/CalendarView.ts"
      via: "plugin.settings.defaultView"
      pattern: "this\\.plugin\\.settings\\.defaultView"
---

<objective>
Fix three code-level gaps identified in v1.0 milestone audit: wire defaultView setting to CalendarView, add leap month "闰" prefix, and fix 2 failing all-day event end date tests.

Purpose: Close all code/test gaps so the test suite is green and settings work as users expect.
Output: 3 source file fixes, 2 test file fixes, 0 test failures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire defaultView setting and fix leap month display</name>
  <files>src/views/CalendarView.ts, src/lunar/LunarService.ts</files>
  <read_first>
    - src/views/CalendarView.ts (line 617: hardcoded currentViewMode, line 641: onOpen method)
    - src/lunar/LunarService.ts (line 82-86: getLunarMonthForTitle method)
    - src/models/types.ts (line 42: defaultView type, line 97: default value)
    - src/settings/SettingsTab.ts (line 125-131: defaultView dropdown)
  </read_first>
  <action>
**Fix 1 — CalendarView defaultView wiring:**

In `src/views/CalendarView.ts`, change the property declaration at line 617 from:
```typescript
private currentViewMode: 'month' | 'week' | 'day' = 'month';
```
to:
```typescript
private currentViewMode: 'month' | 'week' | 'day' = 'month';
```
(keep the default as fallback)

Then in the `onOpen()` method (line 641), immediately after `const container = this.containerEl.children[1] as HTMLElement;` add:
```typescript
this.currentViewMode = this.plugin.settings.defaultView;
```

This ensures CalendarView reads the user's defaultView setting each time the view opens, while keeping 'month' as a safe fallback for the property initializer.

**Fix 2 — Leap month "闰" prefix:**

In `src/lunar/LunarService.ts`, replace the `getLunarMonthForTitle` method (lines 82-86) with:
```typescript
getLunarMonthForTitle(year: number, month: number): string {
  const dateStr = this.formatDate(year, month, 1);
  const lunar = getLunarDate(dateStr);
  return lunar.isLeap ? `闰${lunar.lunarMonCN}` : lunar.lunarMonCN;
}
```

This checks the `isLeap` boolean from chinese-days and prepends "闰" when the 1st of the Gregorian month falls in a lunar leap month.
  </action>
  <verify>
    <automated>cd C:/OneDrive/src/UniCalendar && npx vitest run tests/lunar/LunarService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/views/CalendarView.ts contains `this.currentViewMode = this.plugin.settings.defaultView`
    - The line `this.currentViewMode = this.plugin.settings.defaultView` appears inside the `onOpen()` method body
    - src/lunar/LunarService.ts contains `lunar.isLeap ? `
    - src/lunar/LunarService.ts getLunarMonthForTitle method contains the string `闰`
    - LunarService tests pass (npx vitest run tests/lunar/LunarService.test.ts exits 0)
  </acceptance_criteria>
  <done>CalendarView reads defaultView setting on open; getLunarMonthForTitle prepends "闰" for leap months; LunarService tests green</done>
</task>

<task type="auto">
  <name>Task 2: Fix all-day event end date test expectations</name>
  <files>tests/sync/IcsSyncAdapter.test.ts, tests/sync/GoogleSyncAdapter.test.ts</files>
  <read_first>
    - tests/sync/IcsSyncAdapter.test.ts (lines 99-110: all-day event test)
    - tests/sync/GoogleSyncAdapter.test.ts (lines 160-182: all-day event test)
    - src/sync/IcsSyncAdapter.ts (lines 127-130: exclusive-to-inclusive conversion comment)
    - src/sync/GoogleSyncAdapter.ts (lines 126-131: exclusive-to-inclusive conversion)
  </read_first>
  <action>
The implementation correctly converts exclusive end dates (iCalendar/Google standard) to inclusive end dates (UniCalendar internal convention). The tests incorrectly expect the original exclusive dates.

**Fix IcsSyncAdapter.test.ts** (line 109):
Change:
```typescript
expect(event.end).toContain('2026-04-02');
```
to:
```typescript
expect(event.end).toContain('2026-04-01');
```
The ICS input has DTEND:20260402 (exclusive), but the adapter calls `prevDay()` to normalize to 2026-04-01 (inclusive). The test should expect the inclusive date.

Also update the comment on line 107 to clarify:
```typescript
// All-day events: adapter normalizes exclusive DTEND to inclusive end date
```

**Fix GoogleSyncAdapter.test.ts** (line 181):
Change:
```typescript
expect(events[0]!.end).toBe('2026-04-06');
```
to:
```typescript
expect(events[0]!.end).toBe('2026-04-05');
```
The mock Google event has `end: { date: '2026-04-06' }` (exclusive), but the adapter subtracts 1 day to normalize to 2026-04-05 (inclusive). The test should expect the inclusive date.

Also update or add a comment near line 179:
```typescript
// All-day events: adapter normalizes exclusive end date to inclusive
```
  </action>
  <verify>
    <automated>cd C:/OneDrive/src/UniCalendar && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - tests/sync/IcsSyncAdapter.test.ts line containing `event.end` in the all-day test expects '2026-04-01' (not '2026-04-02')
    - tests/sync/GoogleSyncAdapter.test.ts line containing `events[0]!.end` expects '2026-04-05' (not '2026-04-06')
    - Full test suite passes: `npx vitest run` exits 0 with 0 failures
    - Test count remains 97 (no tests removed, no tests added)
  </acceptance_criteria>
  <done>Both all-day event end date tests fixed to expect inclusive dates; full test suite passes with 97 tests, 0 failures</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan fixes existing code and test expectations only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | T (Tampering) | defaultView setting | accept | Setting value is already validated by SettingsTab dropdown (only 'month'/'week'/'day' options); TypeScript union type enforces at compile time |
</threat_model>

<verification>
1. `npx vitest run` exits 0 with 97 passed, 0 failed
2. `grep -n "this.plugin.settings.defaultView" src/views/CalendarView.ts` shows match in onOpen
3. `grep -n "isLeap" src/lunar/LunarService.ts` shows match in getLunarMonthForTitle
</verification>

<success_criteria>
- All 97 tests pass with 0 failures
- CalendarView.onOpen reads plugin.settings.defaultView
- LunarService.getLunarMonthForTitle checks isLeap and prepends "闰"
- No new test regressions introduced
</success_criteria>

<output>
After completion, create `.planning/phases/09-gap-closure/09-01-SUMMARY.md`
</output>
