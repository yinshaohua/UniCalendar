---
phase: 05-apple-calendar-ui-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/styles/calendar-view.css
autonomous: false
requirements:
  - INFR-03

must_haves:
  truths:
    - "Legacy calendar-view.css is removed if confirmed unused"
    - "Calendar UI looks polished in both light and dark Obsidian themes"
    - "Event bars and blocks appear as floating cards with subtle shadows"
    - "Grid lines recede behind event content visually"
    - "Color palette swatches render as clickable circles in source modals"
    - "All visual changes work on both desktop and mobile"
  artifacts:
    - path: "src/views/CalendarView.ts"
      provides: "Polished calendar CSS and inline styles"
      contains: "border-radius: 8px"
    - path: "src/views/EventDetailModal.ts"
      provides: "Modal with shadow and fade-in"
      contains: "uni-fade-in"
    - path: "src/settings/SettingsTab.ts"
      provides: "Settings with card shadows and palette picker"
      contains: "uni-calendar-palette-swatch"
  key_links:
    - from: "All CSS changes"
      to: "Visual output in Obsidian"
      via: "Human verification in running plugin"
      pattern: "visual inspection"
---

<objective>
Clean up legacy CSS file and visually verify the complete Apple Calendar UI polish in Obsidian.

Purpose: Confirm all visual changes from plans 01 and 02 render correctly in the actual Obsidian environment, and remove dead code.
Output: Removed legacy CSS (if unused), verified visual quality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-apple-calendar-ui-polish/05-CONTEXT.md
@.planning/phases/05-apple-calendar-ui-polish/05-01-SUMMARY.md
@.planning/phases/05-apple-calendar-ui-polish/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy calendar-view.css and verify build</name>
  <files>src/styles/calendar-view.css</files>
  <read_first>
    - src/styles/calendar-view.css (check contents — expected ~119 lines of unused CSS)
    - src/main.ts (verify no import of calendar-view.css)
    - src/views/CalendarView.ts (verify no import of calendar-view.css)
  </read_first>
  <action>
Search the entire codebase for any import or reference to `calendar-view.css` or `src/styles/calendar-view.css`. Check:
- `src/main.ts` for any `import './styles/calendar-view.css'` or similar
- `esbuild.config.mjs` for any CSS loader or reference
- All `.ts` files for any reference to this file

If NO references are found: delete `src/styles/calendar-view.css`.
If references ARE found: do NOT delete. Instead, document which files reference it and leave as-is.

After deletion (if applicable), run `npm run build` to confirm the build still succeeds.
  </action>
  <verify>
    <automated>cd C:/OneDrive/src/UniCalendar && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - If deleted: `src/styles/calendar-view.css` does not exist AND `npm run build` exits with code 0
    - If kept: a comment in the SUMMARY explains why it was retained
    - No TypeScript or build errors
  </acceptance_criteria>
  <done>Legacy CSS file removed (or documented reason for keeping), build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Apple Calendar UI polish</name>
  <what-built>
Complete Apple Calendar-inspired visual polish across all UI surfaces:
- CalendarView: rounded event bars (6px) and blocks (8px), subtle shadows with hover lift, softened grid lines, toolbar shadow, smooth transitions, unified 15% color-mix backgrounds
- EventDetailModal: elevated shadow, fade-in animation, refined spacing
- SettingsTab: card shadows, recommended color palette swatches in Add/Edit modals
  </what-built>
  <how-to-verify>
1. Run `npm run build` in the project root
2. Copy `main.js`, `manifest.json`, `styles.css` (if exists) to your Obsidian vault's `.obsidian/plugins/uni-calendar/` directory
3. Reload Obsidian (Ctrl+R / Cmd+R)

**CalendarView checks:**
4. Open the UniCalendar view — verify toolbar has subtle bottom shadow separating it from the grid
5. Navigate to a month with events — verify event bars have rounded corners and subtle shadow
6. Hover over an event bar — verify shadow increases (lift effect) and background slightly darkens
7. Switch to week view — verify event blocks have rounded corners (8px), shadow, and border-left accent
8. Hover over an event block — verify shadow lifts
9. Observe grid lines — they should be visibly softer than headers/labels (half-opacity)
10. Check that day headers and hour labels still have strong borders (structural anchors)
11. Verify today's cell highlight is visible but subtle

**EventDetailModal checks:**
12. Click any event to open the detail modal — verify it fades in smoothly
13. Modal should have noticeable elevation shadow

**Settings checks:**
14. Open plugin settings — verify source cards have subtle shadow
15. Click "Add Source" — in the source form, verify a row of 8 colored circle swatches appears below the color picker
16. Click a swatch — verify the color picker updates to match
17. Click "Edit" on an existing source — verify the same palette swatches appear

**Theme checks:**
18. Switch Obsidian to dark theme — verify shadows are subtle (not harsh), event fills are visible but soft
19. Switch back to light theme — verify same quality

**Mobile (if available):**
20. If you have Obsidian mobile, load the plugin and verify the visual changes look good on a smaller screen
  </how-to-verify>
  <resume-signal>Type "approved" to complete the phase, or describe any visual issues that need fixing</resume-signal>
</task>

</tasks>

<verification>
- Build succeeds
- Legacy CSS removed (or documented)
- Human confirms visual quality matches Apple Calendar-inspired design target
- Works in both light and dark themes
</verification>

<success_criteria>
All visual polish verified by human in running Obsidian. The calendar looks polished with rounded corners, subtle shadows, softened grids, and color palette swatches. INFR-03 requirement satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/05-apple-calendar-ui-polish/05-03-SUMMARY.md`
</output>
